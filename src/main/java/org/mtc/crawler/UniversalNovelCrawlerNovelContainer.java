package org.mtc.crawler;

/**
 * 通用型爬虫的小说暂存类
 */
public class UniversalNovelCrawlerNovelContainer {

	/*
	 * 	按顺序储存每一章链接和正文的Map
	 * 	当前分发任务的计数
	 * 	当前已经写入的任务的计数
	 *  
	 * 	领取章节任务的方法（同步）
	 * 	提交章节任务的方法（同步）
	 */
}
